## 1. Correct the Spec Purpose

- [x] 1.1 Rewrite only the `## Purpose` body of `openspec/specs/release-notes-workflow/spec.md` so it describes the repository-local agent skill, bundled helper evidence preparation, and the exact-content inspection gate, without referring to a constrained local Pi process.
- [x] 1.2 Verify the requirement sections and every scenario are byte-identical to before the edit and that `openspec validate --specs` still passes.